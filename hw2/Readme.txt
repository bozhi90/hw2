1.
接腳 : 
	上按鈕(D10)
	下按鈕(D9)
	確認按鈕(D8)
	輸出波型(D7)
	接收波型(A0)
	LCD(D0、D1、D2)

將hw2.cpp、FFT.py和uLCD資料庫放在同一資料夾，

先執行hw2.cpp，此時uLCD畫面會顯示freq : 50 Hz，可利用上下按鈕選擇50Hz或是100Hz，
此時執行FFT.cpp，再按下確認按鈕開始輸出波型，可得到波型圖與FFT，
若要執行下一次輸出請按rst，並重複上述步驟。

2.
輸出結果如資料夾中的圖片，可以精準的判斷出頻率。
